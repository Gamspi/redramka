import React from 'react';

import CardsList from '../../../Core/components/cardsList/CardsList';

/**
 * Страница со списком авиалиний
 */
const AirLineList: React.FC = () => <CardsList />;

export default React.memo(AirLineList);
